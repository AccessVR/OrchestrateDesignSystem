**Input** — single-line text field with optional label, hint, error, and left icon. White, hairline border, soft orange focus ring.

```jsx
<Input label="Work email" type="email" placeholder="you@org.edu" />
<Input label="Org" error="This field is required" />
```
