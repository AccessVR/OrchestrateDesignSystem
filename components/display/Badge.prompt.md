**Badge** — small uppercase status / category pill. Quiet by default; reach for `brand` or `ink` only for real emphasis.

```jsx
<Badge>New</Badge>
<Badge tone="brand">Live</Badge>
<Badge tone="soft">Beta</Badge>
```

Tones: `neutral`, `brand`, `soft`, `ink`, `success`, `warning`.
